import { Empmasterlistmodel } from './empmasterlistmodel';

describe('Empmasterlistmodel', () => {
  it('should create an instance', () => {
    expect(new Empmasterlistmodel()).toBeTruthy();
  });
});
