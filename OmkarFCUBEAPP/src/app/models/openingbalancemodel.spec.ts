import { Openingbalancemodel } from './openingbalancemodel';

describe('Openingbalancemodel', () => {
  it('should create an instance', () => {
    expect(new Openingbalancemodel()).toBeTruthy();
  });
});
