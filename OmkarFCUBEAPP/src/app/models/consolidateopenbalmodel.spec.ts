import { Consolidateopenbalmodel } from './consolidateopenbalmodel';

describe('Consolidateopenbalmodel', () => {
  it('should create an instance', () => {
    expect(new Consolidateopenbalmodel()).toBeTruthy();
  });
});
