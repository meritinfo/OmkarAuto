import { Emppaycallistmodel } from './emppaycallistmodel';

describe('Emppaycallistmodel', () => {
  it('should create an instance', () => {
    expect(new Emppaycallistmodel()).toBeTruthy();
  });
});
