import { Exptruckarrivalmodel } from './exptruckarrivalmodel';

describe('Exptruckarrivalmodel', () => {
  it('should create an instance', () => {
    expect(new Exptruckarrivalmodel()).toBeTruthy();
  });
});
