import { UppercaseonlyDirective } from './uppercaseonly.directive';

describe('UppercaseonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new UppercaseonlyDirective();
    expect(directive).toBeTruthy();
  });
});
