import { DecimalonlyDirective } from './decimalonly.directive';

describe('DecimalonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalonlyDirective();
    expect(directive).toBeTruthy();
  });
});
