import { Dovehicleinlistmodel } from './dovehicleinlistmodel';

describe('Dovehicleinlistmodel', () => {
  it('should create an instance', () => {
    expect(new Dovehicleinlistmodel()).toBeTruthy();
  });
});
