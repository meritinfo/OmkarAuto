import { Consignmentinvmodel } from './consignmentinvmodel';

describe('Consignmentinvmodel', () => {
  it('should create an instance', () => {
    expect(new Consignmentinvmodel()).toBeTruthy();
  });
});
