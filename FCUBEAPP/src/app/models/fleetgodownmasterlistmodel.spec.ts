import { fleetgodownmasterlistmodel } from './fleetgodownmasterlistmodel';

describe('Gleetgodownmastermodellist', () => {
  it('should create an instance', () => {
    expect(new fleetgodownmasterlistmodel()).toBeTruthy();
  });
});
