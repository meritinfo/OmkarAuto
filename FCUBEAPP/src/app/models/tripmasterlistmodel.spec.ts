import { Tripmasterlistmodel } from './tripmasterlistmodel';

describe('Tripmasterlistmodel', () => {
  it('should create an instance', () => {
    expect(new Tripmasterlistmodel()).toBeTruthy();
  });
});
