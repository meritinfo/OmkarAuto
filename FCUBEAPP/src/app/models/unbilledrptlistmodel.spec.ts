import { Unbilledrptlistmodel } from './unbilledrptlistmodel';

describe('Unbilledrptlistmodel', () => {
  it('should create an instance', () => {
    expect(new Unbilledrptlistmodel()).toBeTruthy();
  });
});
