import { Dovehiplacedlistmodel } from './dovehiplacedlistmodel';

describe('Dovehiplacedlistmodel', () => {
  it('should create an instance', () => {
    expect(new Dovehiplacedlistmodel()).toBeTruthy();
  });
});
