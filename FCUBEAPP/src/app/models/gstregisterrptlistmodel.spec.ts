import { Gstregisterrptlistmodel } from './gstregisterrptlistmodel';

describe('Gstregisterrptlistmodel', () => {
  it('should create an instance', () => {
    expect(new Gstregisterrptlistmodel()).toBeTruthy();
  });
});
