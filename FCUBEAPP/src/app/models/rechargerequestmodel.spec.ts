import { Rechargerequestmodel } from './rechargerequestmodel';

describe('Rechargerequestmodel', () => {
  it('should create an instance', () => {
    expect(new Rechargerequestmodel()).toBeTruthy();
  });
});
