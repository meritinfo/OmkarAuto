import { Dprvehiplacedmodel } from './dprvehiplacedmodel';

describe('Dprvehiplacedmodel', () => {
  it('should create an instance', () => {
    expect(new Dprvehiplacedmodel()).toBeTruthy();
  });
});
