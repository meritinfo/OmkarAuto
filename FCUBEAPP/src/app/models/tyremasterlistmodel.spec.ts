import { Tyremasterlistmodel } from './tyremasterlistmodel';

describe('Tyremasterlistmodel', () => {
  it('should create an instance', () => {
    expect(new Tyremasterlistmodel()).toBeTruthy();
  });
});
