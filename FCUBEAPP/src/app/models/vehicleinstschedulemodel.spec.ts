import { Vehicleinstschedulemodel } from './vehicleinstschedulemodel';

describe('Vehicleinstschedulemodel', () => {
  it('should create an instance', () => {
    expect(new Vehicleinstschedulemodel()).toBeTruthy();
  });
});
