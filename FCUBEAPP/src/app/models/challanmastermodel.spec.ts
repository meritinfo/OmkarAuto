import { Challanmastermodel } from './challanmastermodel';

describe('Challanmastermodel', () => {
  it('should create an instance', () => {
    expect(new Challanmastermodel()).toBeTruthy();
  });
});
