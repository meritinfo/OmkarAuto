import { Bankreconcilationlist } from './bankreconcilationlist';

describe('Bankreconcilationlist', () => {
  it('should create an instance', () => {
    expect(new Bankreconcilationlist()).toBeTruthy();
  });
});
