import { ThreedecimalonlyDirective } from './threedecimalonly.directive';

describe('ThreedecimalonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new ThreedecimalonlyDirective();
    expect(directive).toBeTruthy();
  });
});
