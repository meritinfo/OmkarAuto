import { NegdecimalonlyDirective } from './negdecimalonly.directive';

describe('NegdecimalonlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NegdecimalonlyDirective();
    expect(directive).toBeTruthy();
  });
});
